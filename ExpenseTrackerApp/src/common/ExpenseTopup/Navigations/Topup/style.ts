import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  topupContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    borderWidth: 1,
    borderColor: 'red'
  },
});